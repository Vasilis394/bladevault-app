<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your knives</title>
    <style>
        /* Reset and base styles */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            line-height: 1.6;
            color: #333;
            background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
            min-height: 100vh;
        }

        /* Navigation Styles */
        nav {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            padding: 1rem 2rem;
            
            position: sticky;
            top: 0;
            z-index: 100;
        }

        nav a {
            text-decoration: none;
            color: #2c3e50;
            font-weight: 600;
            margin-right: 2rem;
            padding: 0.5rem 1rem;
            border-radius: 6px;
            transition: all 0.3s ease;
            position: relative;
        }

        /* Main Content */
        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 2rem;
        }

        /* Header */
        .page-header {
            text-align: center;
            margin-bottom: 3rem;
        }

        .page-title {
            font-size: 3rem;
            font-weight: 700;
            margin-bottom: 1rem;
            background: linear-gradient(45deg, #e74c3c);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        /* Filter Section */
        .filter-section {
            background: white;
            padding: 2rem;
            border-radius: 15px;
            
            margin-bottom: 2rem;
            position: relative;
            overflow: hidden;
        }

       

        .search-form, .category-form {
            margin-bottom: 1.5rem;
        }

        .form-group {
            display: flex;
            flex-direction: column;
            gap: 0.5rem;
            margin-bottom: 1rem;
        }

        label {
            color: #2c3e50;
            font-weight: 600;
            font-size: 1rem;
        }

        input, select {
            padding: 0.8rem;
            border: 2px solid #e9ecef;
            border-radius: 8px;
            font-size: 1rem;
            transition: all 0.3s ease;
            background: #f8f9fa;
            max-width: 400px;
        }

        

        /* Buttons */
        .btn {
            padding: 0.8rem 1.5rem;
            border: none;
            border-radius: 8px;
            font-size: 1rem;
            font-weight: 600;
            
            transition: all 0.3s ease;
            text-decoration: none;
            display: inline-block;
            text-align: center;
        }

        .btn-primary {
            background: linear-gradient(45deg, #e74c3c);
            color: white;
            
        }

        

        .btn-secondary {
            background: #6c757d;
            color: white;
            margin-left: 10px;
        }

        

        .add-knife-btn {
            background: linear-gradient(45deg, #27ae60);
            color: white;
            padding: 1rem 2rem;
            margin-bottom: 2rem;
            
        }

        /* Knives List */
        .knives-list {
            background: white;
            padding: 2rem;
            border-radius: 15px;
            
        }

        .knives-list h2 {
            color: #2c3e50;
            margin-bottom: 1.5rem;
            font-size: 1.5rem;
            font-weight: 600;
        }

        .knives-grid {
            list-style: none;
        }

        .knife-item {
            padding: 1rem;
            border: 1px solid #e9ecef;
            border-radius: 8px;
            margin-bottom: 0.5rem;
           
        }


        .knife-link {
            text-decoration: none;
            color: #2c3e50;
            font-weight: 500;
            display: block;
            
        }

       

        .empty-state {
            text-align: center;
            padding: 2rem;
            color: #6c757d;
            font-style: italic;
        }

        
    </style>
</head>
<body>

    <%- include('../partials/_navbar.ejs') %>
    
    <div class="container">
        <header class="page-header">
            <h1 class="page-title">My knives</h1>
        </header>

        <section class="filter-section">
            <form class="search-form" action="/users/<%= user._id %>/knives" method="GET">
                <div class="form-group">
                    <label for="search">Search knives:</label>
                    <input type="text" id="search" name="search" placeholder="Enter knife name..." value="<%= searchTerm %>">
                </div>
                <button class="btn btn-primary" type="submit">Search</button>
                <% if (searchTerm) { %>
                    <a class="btn btn-secondary" href="/users/<%= user._id %>/knives">Clear search</a>
                <% } %>
            </form>

            <form class="category-form" action="/users/<%= user._id %>/knives" method="GET">
                <div class="form-group">
                    <label for="category">Choose a category:</label>
                    <select name="category" id="category" onchange="this.form.submit()">
                        <option value="all" <%= selectedCategory === 'all' ? 'selected' : '' %>>All knives</option>
                        <option value="combat-knife" <%= selectedCategory === 'combat-knife' ? 'selected' : '' %>>Combat knives</option>
                        <option value="kitchen-knife" <%= selectedCategory === 'kitchen-knife' ? 'selected' : '' %>>Kitchen knives</option>
                        <option value="survival-knife" <%= selectedCategory === 'survival-knife' ? 'selected' : '' %>>Survival knives</option>
                        <option value="throwing-knife" <%= selectedCategory === 'throwing-knife' ? 'selected' : '' %>>Throwing knives</option>
                        <option value="automatic-knife" <%= selectedCategory === 'automatic-knife' ? 'selected' : '' %>>Automatic knives</option>
                        <option value="hunting-knife" <%= selectedCategory === 'hunting-knife' ? 'selected' : '' %>>Hunting knives</option>
                    </select>
                </div>
            </form>
        </section>

        <a class="btn add-knife-btn" href="/users/<%=user._id%>/knives/new">Add a knife</a>

        <section class="knives-list">
            <h2>Your Knife Collection</h2>
            <ul class="knives-grid">
                <% knife.forEach((knife)=> { %>
                    <li class="knife-item"> 
                        <a class="knife-link" href="/users/<%= user._id %>/knives/<%= knife._id %>"> 
                            <%= knife.name %> 
                        </a> 
                    </li>
                <% }) %>
            </ul>

            <% if (knife.length === 0) { %>
                <p class="empty-state">No knives found in this category.</p>
            <% } %>
        </section>
    </div>
</body>
</html>