<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add a knife</title>
    <style>
        /* Reset and base styles */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            line-height: 1.6;
            color: #333;
            background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }

        /* Navigation Styles */
        nav {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            padding: 1rem 2rem;
            box-shadow: 0 2px 20px rgba(0, 0, 0, 0.1);
            position: sticky;
            top: 0;
            z-index: 100;
        }

        nav a {
            text-decoration: none;
            color: #2c3e50;
            font-weight: 600;
            margin-right: 2rem;
            padding: 0.5rem 1rem;
            border-radius: 6px;
            
            position: relative;
        }

        

        

    

        /* Main Content */
        main {
            flex: 1;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 2rem;
        }

        /* Form Container */
        .form-container {
            background: white;
            padding: 3rem;
            border-radius: 20px;
            
            width: 100%;
            max-width: 600px;
            position: relative;
            overflow: hidden;
        }

        

        /* Heading */
        .page-title {
            text-align: center;
            color: #2c3e50;
            font-size: 2.5rem;
            margin-bottom: 2rem;
            font-weight: 700;
            background: linear-gradient(45deg, #cf1d09);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        /* Form Styles */
        .knife-form {
            display: flex;
            flex-direction: column;
            gap: 1.5rem;
        }

        .form-group {
            display: flex;
            flex-direction: column;
            gap: 0.5rem;
        }

        label {
            color: #2c3e50;
            font-weight: 600;
            font-size: 1rem;
        }

        input, select, textarea {
            padding: 1rem;
            border: 2px solid #e9ecef;
            border-radius: 8px;
            font-size: 1rem;
            
            background: #f8f9fa;
            font-family: inherit;
        }

        

        textarea {
            min-height: 120px;
            resize: vertical;
        }

        .image-preview {
            margin-top: 1rem;
            text-align: center;
        }

        .image-preview img {
            max-width: 200px;
            max-height: 200px;
            border-radius: 8px;
            
        }

        /* Button Styles */
        .submit-btn {
            background: linear-gradient(45deg, #e74c3c);
            color: white;
            border: none;
            padding: 1rem 2rem;
            border-radius: 8px;
            font-size: 1.1rem;
            font-weight: 600;
            
            
            margin-top: 1rem;
            
        }

       
    </style>
</head>

<body>
    
    <%- include('../partials/_navbar.ejs') %>
    
    <main>
        <div class="form-container">
            <h1 class="page-title">Add new knife</h1>
            <form class="knife-form" action="/users/<%= user._id %>/knives" method="POST" enctype="multipart/form-data">
                <div class="form-group">
                    <label for="name">Knife name:</label>
                    <input type="text" name="name" id="name" required/>
                </div>
                <div class="form-group">
                    <label for="material">Material:</label>
                    <input type="text" name="material" id="material" />
                </div>
                <div class="form-group">
                    <label for="category">Category:</label>
                    <select name="category" id="category">
                        <option value="combat-knife">Combat knife</option>
                        <option value="kitchen-knife">Kitchen knife</option>
                        <option value="survival-knife">Survival knife</option>
                        <option value="throwing-knife">Throwing knife</option>
                        <option value="automatic-knife">Automatic knife</option>
                        <option value="hunting-knife">Hunting knife</option>
                    </select>
                </div>
                <div class="form-group">
                    <label for="description">Description:</label>
                    <textarea name="description" id="description"></textarea>
                </div>

                <div class="form-group">
                    <label for="image">Knife Image:</label>
                    <input type="file" name="image" id="image" accept="image/*" class="file-input" />
                    <div class="image-preview" id="imagePreview"></div>
                </div>

                <button class="submit-btn" type="submit">Add knife</button>
            </form>
        </div>
    </main>

    <script>
        
        document.getElementById('image').addEventListener('change', function(e) {
            const preview = document.getElementById('imagePreview');
            preview.innerHTML = '';
            
            if (this.files && this.files[0]) {
                const reader = new FileReader();
                
                reader.onload = function(e) {
                    const img = document.createElement('img');
                    img.src = e.target.result;
                    preview.appendChild(img);
                }
                
                reader.readAsDataURL(this.files[0]);
            }
        });
    </script>
</body>

</html>